{% set categories = craft.categories.group('testimonialCategories')%}
{% set entries = craft.entries()
  .relatedTo('testimonialCategories').all()
%}
{% set firstDayOfMonth = date('first day of this month') %}

{% set entries = craft.entries.section("testimonials").relatedTo(category) %}
{% for item in entries %}
    {{ dump(item.url) }}
{{ item }}
    
{% endfor %}
{# Fetch the categories #}

<li class="active"><a href="/products">Semua Produk</a></li>
{% nav category in craft.categories.group('testimonialCategories') %}
<li>
    <a href="{{ category.url }}"><i class="fa fa-angle-right"></i> {{ category.title }}</a>
    {% ifchildren %}
        <ul>
            {% children %}
        </ul>
    {% endifchildren %}
</li>
{% endnav %}